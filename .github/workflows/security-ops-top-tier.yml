name: Security and operations finalization

on:
  push:
    branches:
      - top-tier-rebuild-20260904

permissions:
  contents: write

jobs:
  verify:
    if: contains(github.event.head_commit.message, '[security-ops]')
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v6
        with:
          ref: top-tier-rebuild-20260904
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: node scripts/security-ops-finalize.mjs
      - run: npm install --no-audit --no-fund
      - name: Typecheck
        id: typecheck
        continue-on-error: true
        run: npm run typecheck > /tmp/typecheck.log 2>&1
      - name: Test
        id: tests
        continue-on-error: true
        run: npm test > /tmp/tests.log 2>&1
      - name: Build
        id: build
        continue-on-error: true
        run: npm run build > /tmp/build.log 2>&1
      - name: Commit report and source
        if: always()
        shell: bash
        run: |
          {
            echo '# Security and operations verification'
            echo "typecheck=${{ steps.typecheck.outcome }}"
            echo "tests=${{ steps.tests.outcome }}"
            echo "build=${{ steps.build.outcome }}"
            echo '## Typecheck'
            echo '```text'
            tail -n 500 /tmp/typecheck.log 2>/dev/null || true
            echo '```'
            echo '## Tests'
            echo '```text'
            tail -n 320 /tmp/tests.log 2>/dev/null || true
            echo '```'
            echo '## Build'
            echo '```text'
            tail -n 500 /tmp/build.log 2>/dev/null || true
            echo '```'
          } > TOP_TIER_CI_REPORT.md
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: harden avatar email and release operations" || true
          git push origin HEAD:top-tier-rebuild-20260904
      - name: Enforce checks
        if: steps.typecheck.outcome != 'success' || steps.tests.outcome != 'success' || steps.build.outcome != 'success'
        run: exit 1
      - name: Remove one-time automation
        shell: bash
        run: |
          rm -f .github/workflows/*top-tier*.yml scripts/*top-tier-rebuild.mjs scripts/security-ops-finalize.mjs
          git add -A
          git commit -m "chore: remove one-time delivery automation" || true
          git push origin HEAD:top-tier-rebuild-20260904
      - name: Fast-forward main
        run: git push origin HEAD:main

import "../styles/common.css";
import "../styles/routes/station.css";
import "../workspace.css";

export default function AnnouncementsLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return children;
}

import fs from "node:fs";
import path from "node:path";
const root = process.cwd();
const p = (file) => path.join(root, file);
const read = (file) => fs.readFileSync(p(file), "utf8");
const write = (file, value) => { fs.mkdirSync(path.dirname(p(file)), { recursive: true }); fs.writeFileSync(p(file), value.replace(/\r\n?/g, "\n")); };

write("src/components/OriginalMarkdown.tsx", `import type { ComponentPropsWithoutRef, ReactNode } from "react";\nimport ReactMarkdown from "react-markdown";\nimport remarkGfm from "remark-gfm";\n\ntype OriginalMarkdownProps = {\n  markdown?: string;\n  content?: string;\n  body?: string;\n  children?: string;\n  className?: string;\n  headings?: unknown;\n  locale?: unknown;\n};\n\ntype ImageMeta = { src: string; width?: number; height?: number; caption?: string };\n\nfunction safeHref(value: string | undefined): string | undefined {\n  if (!value || /[\\r\\n\\0]/u.test(value)) return undefined;\n  if (value.startsWith("/") && !value.startsWith("//")) return value;\n  try {\n    const parsed = new URL(value);\n    return parsed.protocol === "http:" || parsed.protocol === "https:" || parsed.protocol === "mailto:" ? parsed.toString() : undefined;\n  } catch {\n    return undefined;\n  }\n}\n\nfunction extractHeadingIds(markdown: string): string[] {\n  return [...markdown.matchAll(/<!-- original-heading:(heading_[A-Za-z0-9_-]{8,80}) -->\\s*\\n#{2,3}\\s/gu)].map((match) => match[1]);\n}\n\nfunction extractImages(markdown: string): ImageMeta[] {\n  const lines = markdown.split("\\n");\n  const result: ImageMeta[] = [];\n  for (let index = 0; index < lines.length; index += 1) {\n    const match = lines[index].match(/^!\\[([^\\]]*)\\]\\((\\/original\\/assets\\/[1-9]\\d*)(?:\\s+"([^"]*)")?\\)$/u);\n    if (!match) continue;\n    const size = lines[index + 1]?.match(/^<!-- original-image-size:([1-9]\\d*)x([1-9]\\d*) -->$/u);\n    result.push({ src: match[2], caption: match[3] || "", width: size ? Number(size[1]) : undefined, height: size ? Number(size[2]) : undefined });\n  }\n  return result;\n}\n\nfunction Heading({ level, id, children, ...props }: ComponentPropsWithoutRef<"h2"> & { level: 2 | 3; id?: string }) {\n  if (level === 3) return <h3 id={id} {...props}>{children}</h3>;\n  return <h2 id={id} {...props}>{children}</h2>;\n}\n\nexport function OriginalMarkdown(props: OriginalMarkdownProps) {\n  const markdown = String(props.markdown ?? props.content ?? props.body ?? props.children ?? "").replace(/\\r\\n?/g, "\\n");\n  const headingIds = extractHeadingIds(markdown);\n  const imageMeta = extractImages(markdown);\n  let headingIndex = 0;\n  let imageIndex = 0;\n  return (\n    <div className={props.className || "originalMarkdown"}>\n      <ReactMarkdown\n        remarkPlugins={[remarkGfm]}\n        skipHtml\n        urlTransform={(url) => safeHref(url) || ""}\n        components={{\n          h1: ({ children }) => <h2>{children}</h2>,\n          h2: ({ children, ...elementProps }) => <Heading level={2} id={headingIds[headingIndex++]} {...elementProps}>{children}</Heading>,\n          h3: ({ children, ...elementProps }) => <Heading level={3} id={headingIds[headingIndex++]} {...elementProps}>{children}</Heading>,\n          a: ({ href, children, ...elementProps }) => {\n            const safe = safeHref(href);\n            return safe ? <a href={safe} rel={safe.startsWith("/") ? undefined : "nofollow noopener noreferrer"} {...elementProps}>{children}</a> : <span>{children}</span>;\n          },\n          img: ({ src, alt }) => {\n            const expected = imageMeta[imageIndex++];\n            const safe = typeof src === "string" && /^\\/original\\/assets\\/[1-9]\\d*$/u.test(src) ? src : "";\n            if (!safe) return <span className="originalImageUnavailable">{alt || "外部图片已拦截"}</span>;\n            const meta = expected?.src === safe ? expected : { src: safe };\n            return (\n              <figure className="originalArticleImage">\n                {/* eslint-disable-next-line @next/next/no-img-element */}\n                <img src={safe} alt={alt || ""} width={meta.width} height={meta.height} loading="lazy" decoding="async" />\n                {meta.caption ? <figcaption>{meta.caption}</figcaption> : null}\n              </figure>\n            );\n          },\n          p: ({ children, ...elementProps }) => <p {...elementProps}>{children as ReactNode}</p>,\n        }}\n      >\n        {markdown}\n      </ReactMarkdown>\n    </div>\n  );\n}\n\nexport default OriginalMarkdown;\n`);

const serialization = "src/features/original-editor/serialization.ts";
if (fs.existsSync(p(serialization))) {
  write(serialization, read(serialization).replace(/  if \(format & 4\) text = `<u>\$\{text\}<\\\/u>`;\n/, ""));
}

const cssFile = "src/app/original.css";
if (fs.existsSync(p(cssFile))) {
  const source = read(cssFile);
  const patch = `\n\n/* Dedicated long-form article typography and stable uploaded media. */\n.originalArticlePage .readerNovelNavigation,\n.originalArticleReader .readerNovelNavigation,\n.originalArticlePage .readerChapterNavigation { display: none !important; }\n.originalArticleBody,\n.originalMarkdown {\n  width: min(100%, 704px);\n  margin-inline: auto;\n  font-family: var(--original-reading-font, -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "PingFang TC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans CJK SC", sans-serif);\n  font-size: 17px;\n  line-height: 1.8;\n  white-space: normal;\n  word-break: break-word;\n  overflow-wrap: anywhere;\n  text-indent: 0;\n}\n.originalMarkdown p { margin: 0 0 .95em; white-space: normal; }\n.originalMarkdown h2 { margin: 2.1em 0 .72em; font-size: 24px; line-height: 1.42; font-weight: 680; scroll-margin-top: 82px; }\n.originalMarkdown h3 { margin: 1.75em 0 .62em; font-size: 20px; line-height: 1.5; font-weight: 670; scroll-margin-top: 82px; }\n.originalMarkdown pre { white-space: pre; overflow-x: auto; }\n.originalArticleImage { margin: 28px 0; text-align: center; }\n.originalArticleImage img { display: block; width: auto; max-width: 100%; height: auto; margin: 0 auto; border-radius: 8px; background: color-mix(in srgb, currentColor 5%, transparent); }\n.originalArticleImage figcaption { margin-top: 9px; color: var(--textMuted, #747982); font-size: 13px; line-height: 1.55; }\n.originalImageUnavailable { display: block; min-height: 72px; padding: 22px; border: 1px dashed color-mix(in srgb, currentColor 20%, transparent); border-radius: 8px; color: var(--textMuted, #747982); text-align: center; }\n@media (max-width: 720px) {\n  .originalArticleBody, .originalMarkdown { width: 100%; font-size: 17px; line-height: 1.82; }\n  .originalMarkdown h2 { font-size: 22px; }\n  .originalMarkdown h3 { font-size: 19px; }\n  .originalArticleImage { margin: 24px 0; }\n}\n`;
  if (!source.includes("Dedicated long-form article typography")) write(cssFile, `${source.trimEnd()}${patch}\n`);
}

console.log("Original reading renderer finalized.");
